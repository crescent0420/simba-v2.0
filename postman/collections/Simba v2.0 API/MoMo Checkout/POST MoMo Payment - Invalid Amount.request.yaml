$kind: http-request
url: "{{baseURL}}/api/checkout/momo"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "phone": "0781234567",
      "pin": "1234",
      "amount": -100
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 400", function () {
          pm.response.to.have.status(400);
      });
      pm.test("Error message mentions amount", function () {
          const json = pm.response.json();
          pm.expect(json.error).to.include('amount');
      });
    language: text/javascript
order: 5000
